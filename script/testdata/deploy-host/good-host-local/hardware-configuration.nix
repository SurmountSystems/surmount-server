# Synthetic hardware-configuration placeholder for deploy-host fixtures.
# Real hosts generate this on the machine (nixos-generate-config / disko).
{ ... }:
{
  # empty stub for layout presence only
}
